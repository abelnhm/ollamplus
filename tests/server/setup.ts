// Setup específico para tests del servidor
// No requiere jsdom ya que es código Node.js

export const setupServer = () => {
  // Configuración específica para tests de servidor
  // No se necesita jsdom porque el servidor no usa el DOM
};

setupServer();